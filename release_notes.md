# AI Image Studio リリースノート (Release Notes)
# AI Image Studio リリースノート (Release Notes)

このドキュメントは、「AI Image Studio」の開発開始（2026年7月10日）から現在までのバージョン変更履歴、追加機能、バグ修正、およびバージョン情報をまとめたものです。

## バージョニングの適用ルール
* **1.0.0**: 最初のコーディング完了（基本機能の実装）
* **0.1**: 機能追加によるマイナーアップデート (Y を 1 増やし、Z を 0 にリセット)
* **0.0.1**: 細かな修正、レイアウト調整、バグ修正によるパッチアップデート (Z を 1 増やす)
* **1.0.0**: ソースコード全体の書き直し、大規模リファクタリングによるメジャーアップデート (X を 1 増やし、Y と Z を 0 にリセット)

---

## バージョン履歴サマリー (Version History Summary)

|  バージョン  |  リリース日  |  分類  |  主な変更内容  |  対応コミット/対話  |
| :--- | :--- | :--- | :--- | :--- |
| **4.9.2** | 2026-07-28 | バグ修正・ガードレール強化 | LLM（Gemini / OpenAI等）によるプロンプト精製・翻訳時に応答結果が空文字になった場合、および画像生成API（fal.ai等）に空プロンプトが渡された際の422エラーを防ぐ安全ガードレールを導入。空プロンプト時は入力プロンプト/前文脈への動的フォールバックおよび直前検証を追加。 | 現在の対話 |
| **4.9.1** | 2026-07-27 | バグ修正・DB最適化 | Galleryでのサムネイル二重表示不具合およびタイムスタンプの表記ズレを根本修正。ドライブ文字の大小差異（`C:\` と `c:\`）に起因する二重登録判定を解消するパス正規化（`normalize_path`）および `filename` 重複チェックを実装。DB内の既存重複レコード52件を自動クリーンアップ（削除）し、タイムスタンプをJST刻印日時へ統一補完。 | 現在の対話 |
| **4.9.0** | 2026-07-27 | マイナーアップデート・機能拡張 | 画像編集モード (Image Editing) の本実装および文脈連動機能の導入。OpenAIプロバイダでの画像編集時にソース画像を無視して新規生成に置き換わっていた不具合を解消し、`VisionClient` によるマルチモーダル特徴抽出＋編集指示プロンプト統合（Vision Prompt Fusion）を構築。また、`GenerationService` にて編集モード実行時に元画像の文脈（人物像・構図）を自律取得・維持する仕組みを導入し、プロンプトの先頭不要カンマ除去を実装。 | 現在の対話 |
| **4.8.4** | 2026-07-27 | 新機能・UI機能拡張 | Galleryパネルに「並び替え (Sort)」ツールバーコントロールを追加。📅日時が新しい順 (Newest First - デフォルト)、📅日時が古い順 (Oldest First)、⭐お気に入り優先 (Favorites First)、🔤ファイル名順 (Name A-Z) の4種類のソートに対応し、選択時に画像を再読み込みすることなく瞬時にグリッド表示を整列。 | 現在の対話 |
| **4.8.3** | 2026-07-27 | 機能改善・UI UX向上 | Galleryパネルのバックグラウンド自動ウォームアップ非同期化（QThread）とタブ状態インジケーター（グレーアウト機能）を導入。起動後自動的に裏で画像を読み込み、読み込み中はGalleryタブをグレーアウト（`🖼️ Gallery (読み込み中...)`）させて初回クリック時のプチフリーズを完全解消。 | 現在の対話 |
| **4.8.2** | 2026-07-27 | パフォーマンス改善 | GUI起動時間を約106秒から数秒（UI初期構築2.8秒）へ劇的高速化。Galleryタブ・ModelCatalogタブの Lazy Loading（遅延初期化）、PromptPanel での ExpertModelPanel オンデマンド生成、outputs 出力走査（import_legacy_history）の非同期バックグラウンド処理化を導入。 | 現在の対話 |
| **4.8.1** | 2026-07-27 | CI/CD・バグ修正 | GitHub Actions ワークフロー設定（`.github/workflows/ci.yml` および `release.yml`）で指定されていた未リリースのActionバージョン（`actions/checkout@v5` -> `v4`、`actions/setup-python@v6` -> `v5`）を修正し、GitHubへのPush時に自動テストおよびリリースビルドが正常に動作するよう不具合を解消。 | 現在の対話 |
| **4.8.0** | 2026-07-25 | 新機能・プロバイダー拡張 | 無検閲マルチモーダルAIゲートウェイ「HotAPI」をプロバイダーとして統合。`z-image-spicy`, `seedream-5.0-lite-spicy`, `seedream-5.0-pro-spicy`, `qwen-image-edit-spicy`, `face-swap-spicy` の5モデルを登録し、`NSFW対応`タグフィルタ、ローカル画像アップローダー（`/v1/uploads/`）、非同期タスクポーリング、Face Swap専用パラメータ、設定画面でのAPIキー管理・残高設定、および単体テストを追加。 | 現在の対話 |
| **4.7.7** | 2026-07-25 | 総合バグ修正・安定性向上 | 操作手順により発生していた強制終了、終了時のワーカー競合、ジョブ二重取得、履歴・設定・テンプレート破損、巨大画像によるメモリ枯渇、編集API入力不整合、モデル・品質設定の誤適用を包括的に修正。 | 現在の対話 |
| **4.7.6** | 2026-07-24 | バグ修正 | Galleryパネルのサムネイル画像およびタイトルラベルにマウスイベント透過（`WA_TransparentForMouseEvents`）を設定し、ダブルクリック時に標準ビューアで画像が正しく開かない不具合を修正。あわせて右側プレビュー画像のダブルクリック起動にも対応。 | 現在の対話 |
| **4.7.5** | 2026-07-24 | バグ修正 | アプリ起動時に過去の未完了・復旧ジョブ（`Running`/`Pending`）が自動的にプロンプト精製・画像生成を開始してしまう不具合を修正。起動時に過去セッションの残存ジョブを`Paused`状態へ移行し、Job Queue Managerパネルから手動で再開（`Resume`）できるボタンを追加。 | 現在の対話 |
| **4.7.4** | 2026-07-23 | バグ修正・信頼性向上 | 異常終了で残留した`Running`ジョブを起動時に`Pending`へ復旧。キューワーカーを時間制限付きで停止できるデーモンスレッドへ変更し、API処理中のアプリ終了が無期限にブロックされる問題を修正。画像履歴へ`model_id`を永続化し、既存の隣接JSONからSQLiteへ補完する移行処理を追加して、履歴からの再生成時に元モデルが失われる問題を修正。 | 現在の対話 |
| **4.7.3** | 2026-07-22 | パフォーマンス改善・フリーズ解消 | Gallery（`GalleryPanel`）で画像を削除する際に発生していた一時的な「応答なし」フリーズを解消。削除時に全件サムネイル画像（QPixmap）を不要に二重全リロードしていた処理を改め、対象カードの即時破棄と軽量なグリッド再配置（`rearrange_grid`）方式に最適化し、`QApplication.processEvents()` を組み込むことで応答性を劇的に向上。 | 現在の対話 |
| **4.7.2** | 2026-07-22 | モデル追加・修正 | fal.aiの最新モデル `FLUX.2 Pro` (`fal-ai/flux-2-pro`) をモデルレジストリおよびエキスパートUIに正式復帰統合。 | 現在の対話 |
| **4.7.1** | 2026-07-22 | 精度向上・モデル整理 | fal.aiの公式ドキュメントおよびAPIエンドポイントの実態調査を行い、架空・非存在エンドポイント（Midjourney、Pony、Illustrious、Animagine、Anything等の単体APIエンドポイント）を完全に削除。fal.aiで公式提供されている実在の正確なモデルエンドポイント（FLUX 1.1 Pro `fal-ai/flux-pro/v1.1`, Fooocus `fal-ai/fooocus`, Recraft V3 `fal-ai/recraft-v3`, Ideogram V3 `fal-ai/ideogram-v3`）に整理統合。 | 現在の対話 |
| **4.7.0** | 2026-07-22 | 新機能・UI機能拡張 | モデルカタログ（`ModelCatalogPanel`）に「万能」「アニメイラスト向け」「実写向け」「NSFW対応」のカラフルな属性タグバッジおよび詳細概要文を追加。各モデルカードへのマウスオーバー（ホバー）時に特徴・解説をリッチ表示するツールチップ（QToolTip）機能を搭載。さらに検索ツールバーにタグ別絞り込みフィルタを追加。 | 現在の対話 |
| **4.6.0** | 2026-07-22 | 新機能・モデル拡張 | fal.ai プロバイダ用の新しい10種類のモデル（FLUX.1 [schnell], FLUX.1 [dev], Pony Diffusion v6 XL, Fast SDXL, Stable Diffusion v1.5, FLUX-LoRA, Illustrious XL, Animagine XL 3.1, Anything v5, NijiJourney / Midjourney, FLUX-LoRA Anime）をレジストリへ追加統合。各モデル固有の推論ステップ数・Guidance Scale・LoRA URL指定等のカスタムパラメータUIを完璧に構成。 | 現在の対話 |
| **4.5.2** | 2026-07-22 | バグ修正・UI視認性向上 | モデルカタログ (`ModelCatalogPanel`) およびTips画面 (`TipsPanel`) のハードコードされていたダークテーマ固定スタイルを削除し、LIGHTモード設定時に正常な白背景・黒テキストテーマへ切り替わるよう不具合修正。あわせて、ダークモード時のドロップダウンリスト (`QComboBox`) 展開メニューの文字色をハッキリとした白 (`#ffffff`) に統一指定し視認性を向上。 | 現在の対話 |
| **4.5.1** | 2026-07-22 | バグ修正 | コンテキスト読み込み（Gallery/リテイク等）実行時に、新構造のスタックウィジェット (`expert_stack_widget`) ではなく旧属性名 (`expert_tab_widget`) を参照していたことにより発生する `AttributeError` 例外を修正。 | 現在の対話 |
| **4.5.0** | 2026-07-22 | 大幅UI刷新・機能拡張 | 設定画面の整理・新設「🤖 Model Catalog」タブの追加・生成画面のモデル選択一元化。従来のモデル一覧チェックボックスを設定画面から撤去し独立カタログへ集約（リアルタイム検索・フィルタ・モデルカード表示）。生成画面（PromptPanel）の二重選択タブバーを廃止し、ドロップダウンに操作を完全統合。 | 現在の対話 |
| **4.4.0** | 2026-07-22 | 新機能追加・UI改善 | プロンプト読み込み時のガイドメッセージ（ヒントバナー）機能を導入。通常生成および編集モードに応じて修正・追加入力の案内をプロンプト直下に動的表示し、編集開始または生成開始時に自動で非表示化するスマート案内を統合。 | 現在の対話 |
| **4.3.1** | 2026-07-22 | バグ修正 | 一度画像生成を行った後の再生成（プロンプト改変 / リテイク含む）時に発生する通信例外や属性アクセスエラーによるアプリ強制終了不具合を修正。LLM翻訳の例外フェイルセーフガードを追加し、各種コンポーネントにおける安全な属性チェックを導入。 | 現在の対話 |
| **4.3.0** | 2026-07-22 | 機能統合・UI刷新 | Simple Mode / Expert Mode の切り替え機能を廃止し、UI・操作構造およびパラメータ処理を完全に一本化。モジュール間のモード判定フラグを完全削除し、常にすべてのモデル固有パラメータが正しく読み書き・適用される仕組みへ統一。 | 現在の対話 |
| **4.2.1** | 2026-07-22 | UI・機能改善 | ドラッグ＆ドロップ動作の改善。画像ドロップ時はVision解析やEdit/Visionモードへの自動切り替えを行わず、参照画像（ソース画像）として設定・表示するのみにする操作フローへ改善。 | 現在の対話 |
| **4.2.0** | 2026-07-21 | 機能改善 | Galleryの画像選択を非破壊化。カード選択ではGallery内のプレビューとメタデータ表示だけを更新し、明示的な「コンテキストを読み込む（入力に反映）」操作時にのみ、生成プレビュー・プロンプト・モデル・Expert設定・生成コンテキストへ反映するよう責務を分離。 | 現在の対話 |
| **4.1.9** | 2026-07-19 | バグ修正 | Fal APIでのコンテンツポリシー違反などの生成エラー時、サブスレッドからUIイベントバスへの安全でないシグナル送信を試みたことでアプリ全体が強制終了してしまう不具合を修正。従来の安定したエラー画像（モック）フォールバック表示に安全に戻しました。 | 現在の対話 |
| **4.1.8** | 2026-07-19 | バグ修正 | Fal API 側で生じるデフォルト値補完による Pydantic の型エラー（HTTPValidationError）を回避するため、プロバイダー層でのデフォルト値フェイルセーフ送信処理を追加。同時にUIのパラメータ抽出ロジックを堅牢化し、GUIの設定値が欠落する問題を修正。 | 現在の対話 |
| **4.1.7** | 2026-07-19 | バグ修正 | モデル選択（AI Model）が切り替わった際に、Expert Modeの個別オプションタブ（expert_tab_widget）が連動して切り替わらず、常に一番最初のモデルや無関係なモデルのパラメータ（空、あるいはデフォルト値）が送信されてしまう同期バグを修正。 | 現在の対話 |
| **4.1.6** | 2026-07-19 | バグ修正 | 設定を復元して再生成（リテイク）やLoad Contextを実行した際、Expert Modeの個別パラメータ（safety_tolerance, enable_safety_checker等）がUI上に復元されず、UI表示と実際のAPI送信パラメータが食い違ってしまう重大なバグを修正。 | 現在の対話 |
| **4.1.5** | 2026-07-19 | バグ修正 | 起動時やウィンドウ縦サイズ縮小時に、エキスパートモードの個別オプションエリア（QTabWidget）が潰れて非表示になってしまうバグを最小高さ保証（setMinimumHeight）により修正。 | 現在の対話 |
| **4.1.4** | 2026-07-19 | バグ修正 | ツール起動時に保存されたUIモードとUI上部モード選択トグルボタンの状態が食い違ってしまう初期表示同期バグを修正。 | 現在の対話 |
| **4.1.3** | 2026-07-19 | リファクタリング | 静的解析ツール Ruff / Mypy により検出されたデッドコード、型不整合、未使用変数、PySide6の警告、およびNoneオブジェクトに対する操作によるクラッシュ防止策など計93件の問題を一括修正。 | 現在の対話 |
| **4.1.2** | 2026-07-19 | バグ修正 | Expert ModeのSafety Tolerance（FLUXモデル）において、API仕様の上限である5を超える値「6」がUI上に表示されエラーとなる問題を修正（6を削除しデフォルトを5に変更）。 | 現在の対話 |
| **4.1.1** | 2026-07-19 | バグ修正 | Expert ModeでUIの描画状態（レイアウト再計算など）により、一時的にExpertパラメータがNone（空）で送信されてデフォルト値が適用されてしまうバグを修正。 | 現在の対話 |
| **4.1.0** | 2026-07-18 | 機能追加 | Generation画面下部に常時表示の生成・進捗バーを追加。実行中ジョブ、待機件数、経過時間を表示し、次の待機ジョブを取消可能にした。プレビューには「再生成」（現在の入力）と「設定を復元して再生成」（保存済み設定）の操作を追加し、任意操作を単一リストで管理して後から容易に撤去できるようにした。バージョン表記を共通定数へ一元化。 | 現在の対話 |
| **4.0.3** | 2026-07-18 | バグ修正・調整 | Expert Modeのパラメータが生成APIおよびDBに渡されない致命的なバグの修正、リテイク機能のパラメータ復元漏れの修正、初期ウィンドウサイズの拡大。 | 現在の対話 |
| **4.0.2** | 2026-07-18 | バグ修正 | GUIに Safety Checker が表示されないバグを修正、および StylePreset の内容がメタデータ/DBの Prompt (EN) に反映・表示されないバグの修正。 | 現在の対話 |
| **4.0.1** | 2026-07-18 | 機能追加・修正 | FLUXモデル（FLUX.2 Pro / Dev）に対して `enable_safety_checker` のオプションを追加し、デフォルトをOffに設定。また、空のExpertパラメータ送信時の `TypeError` クラッシュバグを修正。 | 現在の対話 |
| **4.0.0** | 2026-07-17 | メジャーアップデート | UIの大幅なバージョンアップ。Simple Mode と Expert Mode のトグルを追加し、Expert Mode では各AIモデル固有のパラメータをタブ形式で設定できる動的UI（ExpertModelPanel）を実装。 | 現在の対話 |
| **3.5.0** | 2026-07-17 | 機能追加 | StylePreset（スタイルプリセット）の復活。最終英語プロンプトの先頭への自動挿入、統合管理ダイアログ（TemplateDialogにタブ統合）の構築、データベースおよびメタデータ連携。 | 対話 b92945a3 |
| **3.4.1** | 2026-07-17 | バグ修正 | 画像リテイク／プロンプト編集モードで画像生成を実行した際、OpenAiTranslatorで `NameError: name 'style' is not defined` が発生するバグを修正。 | 対話 b92945a3 |
| **3.4.0** | 2026-07-17 | 機能追加 | StyleEngineからTranslation Ruleへの名称変更、およびプロンプト翻訳ルールの外部ファイル化（.mdファイルによる動的読み込み）とUIのラジオボタン化を実装。 | 対話 b92945a3 |
| **3.3.0** | 2026-07-16 | 機能追加 | 画像生成の直前に翻訳・拡張LLM APIをプロキシとして挟む「Style Engine（画風拡張翻訳パイプライン）」を実装。`Standard`, `Photo - Studio`, `Photo - Snap`, `Anime - Cinematic`, `Raw - Direct` の5つのモードから選択可能。 `Anime - Cinematic` には水彩画風の優しいシェーディング・極細線画・マット質感の劇場用アニメ映画スタイルを適用。さらに、メタデータおよびUI（ギャラリー・プレビュー詳細ログ）での生成モデル情報（モデル名、プロバイダー、スタイル）の表示に対応。 | 対話 b92945a3 |
| **3.2.0** | 2026-07-15 | 機能追加 | StylePresetとPrompt Templateのデータ統合。プロンプトへの自動埋め込みや翻訳ロジックのクリーンアップ、UIの整理。 | 対話 b92945a3 |
| **3.1.7** | 2026-07-15 | バグ修正 | OpenAIの一部の独自仕様モデル（gpt-image-2など）で、`quality`パラメータに`hd` / `standard`を送信するとAPIが400エラー（Invalid value）を返すバグを修正。該当モデルに対しては自動的に`high` / `medium`へマッピングして送信するよう調整。 | 対話 77eb5918 |
| **3.1.6** | 2026-07-15 | 機能改善 | クオリティ設定を「Quality (OpenAI)」に表記変更。オプションを「Standard」(standard) と「High Quality」(hd) の2つに整理。OpenAIモデル選択時のみ有効化し、他モデル時はグレーアウトして機能しないよう制御。 | 対話 77eb5918 |
| **3.1.5** | 2026-07-15 | バグ修正 | 画像生成後に選択中のAIモデル・テンプレートがリセットされるバグの修正、およびGalleryからのLoad Context実行時にプロンプト欄が空になってしまうバグの修正。 | 対話 77eb5918 |
| **3.1.4** | 2026-07-15 | バグ修正 | Opus実行計画に基づく検証・クリティカルバグ（リテイク時のモデルID/パス欠落、fal.aiでのマスク無視など）の修正、デッドコード・未使用importの完全クリーンアップ、品質引き継ぎ対応、およびUI表示（残高小数桁数等）の微調整。 | 対話 77eb5918 |
| **3.1.3** | 2026-07-14 | レイアウト調整 | Galleryタブのサムネイル拡大表示（プレビュー画像表示）のサイズを、Generationのサムネイル（450x450）と同じ固定サイズに統一、および中央寄せ配置。 | 対話 d0ceba1c |
| **3.1.2** | 2026-07-14 | レイアウト調整 | PreviewPanel内の3つのアクションボタン（Mark Favorite, Direct Retake, Open Folder）をプレビュー画像の直下に移動、およびメタデータ詳細ログ表示枠を縦に大きく拡張。 | 対話 77eb5918 |
| **3.1.1** | 2026-07-14 | レイアウト調整 | Galleryタブのサムネイル余白削減およびダンプ枠の横幅拡大、ダンプ枠上部への選択画像のスケーリング拡大プレビュー表示の追加。バージョンアップカウントの再開。 | 対話 77eb5918 |
| **3.1.0** | 2026-07-14 | 機能追加 | fal.ai モデル構成の刷新（Grok, FLUX.2, GPT Image 2, Qwen 2.0, Seedream 5.0）、3プロバイダー（OpenAI/fal.ai/Grok）の個別残高シミュレーターの実装、および「💡 Tips」タブ（コスト一覧表、モデル特徴、プロンプトのコツ）の追加。初期ウィンドウサイズを 1280x920 に拡大。 | 対話 77eb5918 |
| **3.0.0** | 2026-07-14 | 全体書き直し | アプリ名を「AI Image Studio」へ変更。OpenAIに加えてfal.aiとxAI (Grok Imagine) をサポートするマルチプロバイダー化。翻訳モデルにGemini (gemini-3.1-flash-lite) を追加。モデル/翻訳プロバイダーの追加・変更が容易なプラグイン調アーキテクチャへのリファクタリング。メイン画面に「生成モード」と「編集モード」のトグルを設置し、画像有無に応じたヒント表示を追加。 | 対話 77eb5918 |
| **2.5.2** | 2026-07-14 | 細かな修正 | Galleryタブ上部の無駄な余白を削減するため上部ツールバー枠を完全削除、「Select All」「Clear Selection」を下部アクションバーに移動、およびアプリバージョンの同期更新。 | 对話 8f90aa7a |
| **2.5.1** | 2026-07-14 | 細かな修正 | 統合後のGalleryタブから「Filter gallery items... (リアルタイム検索)」の検索バーを削除、およびアプリバージョンの同期更新。 | 対話 8f90aa7a |
| **2.5.0** | 2026-07-14 | 機能追加 | GalleryタブとHistoryタブをGalleryタブへと統合。Galleryを3列に縮小し、右側にMetadata Log Dumpとお気に入りフィルタ、「Load Context (入力に反映)」を追加したスプリット画面に変更、不要になったhistory_panel.pyを削除。 | 対話 8f90aa7a |
| **2.4.5** | 2026-07-14 | 細かな修正 | テンプレート管理ダイアログ（TemplateDialog）呼び出し時に QWidget のインポート漏れで NameError が発生するバグの修正、およびアプリバージョンの同期更新 | 対話 8f90aa7a |
| **2.4.4** | 2026-07-14 | 細かな修正 | 配布物からの仮想環境（.venv）の除外による軽量化、および start.bat へのライブラリ自動インストール同意確認（Y/N）プロンプトの実装、アプリバージョンの同期更新 | 対話 e914273e |
| **2.4.3** | 2026-07-14 | 細かな修正 | 配布用 start.bat のカレントディレクトリ・エラーハンドリング・Pythonエイリアス検知・モジュール自動インストール機能の改善、および各ファイルのバージョン表記の統一 | 対話 e914273e |
| **2.4.2** | 2026-07-13 | 細かな修正 | Comparison タブの完全削除、および QSpinBox（Batch Count）の矢印クリックバグを右パディング調整とスパン拡張で修正 | 対話 177e3bd2 |
| **2.4.1** | 2026-07-13 | 細かな修正 | Generation Parameters 内のテンプレート関係ウィジェットの配置（幅）をグリッドに直接配置して統一 | 対話 177e3bd2 |
| **2.4.0** | 2026-07-13 | 機能追加 | StylePresetとTemplatesタブの統合、メイン画面でのプロンプト＆ネガティブテンプレートの直接適用・挿入機能の実装、およびテンプレート管理ダイアログの追加 | 対話 177e3bd2 |
| **2.3.3** | 2026-07-13 | 細かな修正 | アクションボタンの重なり解消（プロンプト高さ175px拡張）、ボタン内の日本語説明削除、パラメータ欄の文字見切れ解消（高さ30px固定・QSSパディング調整）、生成中の「Now Generating」青字・点滅化、初期サイズ拡大 | 対話 bba790d6 |
| **2.3.2** | 2026-07-13 | 細かな修正 | アクションボタン配置（プロンプト右隣）＆ネガティブプロンプト下回り込みレイアウトの完全修正、CodexプレミアムUIテーマおよびヘッダー構造の完全移植 | 対話 bba790d6 |
| **2.3.1** | 2026-07-13 | 細かな修正 | タブ表示のフラット下線化、プロンプト入力欄の高さ増（1行分）、アクションボタン配置アライメント微調整、タイトルバージョン表記更新 | 対話 bba790d6 |
| **2.3.0** | 2026-07-13 | 機能追加 | スタイルプリセットとクイックタグの機能統合、プロンプト直挿入ボタン実装、UIボタンのプレミアムブラッシュアップ | 対話 bba790d6 |
| **2.2.0** | 2026-07-12 | 機能追加 | Project管理機能の強化 (Create/Delete Project実装、アクティブプロジェクト名表示) | `dd4386b` |
| **2.1.1** | 2026-07-12 | 細かな修正 | ボタン配置順の調整、高さクリッピング修正、ギャラリー選択ハイライト追加 | `209ade0` |
| **2.1.0** | 2026-07-12 | 機能追加 | 画像生成後の自動プレビュー反映、ダブルクリックで画像ネイティブオープン機能 | `d02ba7e` |
| **2.0.1** | 2026-07-12 | 細かな修正 | リファクタリング後の画像生成エラー修正、Clear Context動作修正、プレビュークリア処理のデバッグ | `3f26cf9` 後続 |
| **2.0.0** | 2026-07-12 | 全体書き直し | Ver1.1モジュール化アーキテクチャへの大規模リファクタリング | `3f26cf9` |
| **1.5.0** | 2026-07-11 | 機能追加 | ドラッグ＆ドロップによる参照画像編集（Image-to-Imageなど）機能の追加 | 対話 8164097c |
| **1.4.0** | 2026-07-11 | 機能追加 | スタイルプリセット編集のブラッシュアップ（初期化対応）、品質変更ラジオボタン追加 | 対話 8164097c |
| **1.3.1** | 2026-07-11 | 細かな修正 | ギャラリーサムネイル背景ホワイト化、チェック枠の1.25倍拡大 | 対話 8164097c |
| **1.3.0** | 2026-07-10 | 機能追加 | ギャラリー画像の一括選択・削除機能 | 対話 e90986b7 |
| **1.2.1** | 2026-07-10 | 細かな修正 | 画像生成エラーおよびAPIキー連携バグのデバッグ・修正 | 対話 e90986b7 |
| **1.2.0** | 2026-07-10 | 機能追加 | プロンプト/ネガティブテンプレート管理、生成パラメータ保存、リテイク、料金/残高表示、画像比較 | 対話 e90986b7 |
| **1.1.1** | 2026-07-10 | 細かな修正 | 設定画面でのライト/ダークモード切替バグの修正 | 対話 e90986b7 |
| **1.1.0** | 2026-07-10 | 機能追加 | メニューへの設定画面設置、APIキー入力機能、テーマ切替、スタイルプリセット編集の基本実装 | 対話 e90986b7 |
| **1.0.1** | 2026-07-10 | 細かな修正 | 画像生成モデルを dall-e-2 から GPT Image 2 へ修正 | 対話 e90986b7 |
| **1.0.0** | 2026-07-10 | 初期リリース | 新規アプリケーション作成、基本UI構成（プロンプト・プレビュー・ギャラリー等）の実装 | `bc1be14` |

---

## バージョン詳細

### 🚀 v4.9.2 (2026-07-28)
* **分類**: バグ修正・ガードレール強化 (0.0.1アップ)
* **概要**: LLMによるプロンプト精製・翻訳時の空文字応答による画像生成API (fal.ai / xAI 等) の422エラーを防止する多重ガードレールを導入。
* **詳細内容**:
  * **翻訳プロバイダーへのフォールバック実装 (`GeminiTranslator` / `OpenAiTranslator`)**: Gemini や OpenAI などの LLM 応答テキストが空文字列（または空白のみ）になった場合、リクエスト拒否（422 Unprocessable Entity）を回避するために、入力された元の日本語プロンプト (`prompt_jp`) または直前の文脈プロンプト (`prev_en` + `new_jp`) へ動的に安全フォールバックするロジックを追加しました。
  * **生成サービス層での最終プロンプトガード (`GenerationService`)**: 最終的に生成 API に引き渡される英語プロンプト (`prompt_en_final`) が空でないか事前に検証し、空の場合は警告ログを出力した上で `prompt_jp` に差し替える二重ガードレールを導入しました。
  * **画像プロバイダーでの入力検証 (`FalImageProvider`)**: `fal.ai` などの画像プロバイダー層 (`generate` / `edit`) において、空プロンプトで API リクエストが送信されないよう事前検証を行い、無駄な API 通信を発生させずにモックまたは適切なエラー処理に移行するように改善しました。
  * **単体テスト追加**: 空プロンプトに対するフォールバックおよび API バリデーション動作を検証する回帰テスト (`test_empty_prompt_guardrails`) を追加しました。

### 🚀 v4.8.0 (2026-07-25)
* **分類**: 新機能・プロバイダー拡張 (0.1.0アップ)
* **概要**: 無検閲（Uncensored）AIゲートウェイ「HotAPI」をプロバイダーとして新規組み込み、画像生成・画像編集・Face Swapモデルを統合。
* **詳細内容**:
  * **HotAPI プロバイダー実装 (`HotApiProvider`)**: `POST /v1/uploads/` を使用したローカル画像の自動アップロード、`POST /v1/{model}` での非同期タスク送信、`GET /v1/tasks/{task_id}` によるステータスポーリング、生成画像ダウンロード、およびモックフォールバック機構を統合。
  * **無検閲モデルカタログ登録**:
    - `z-image-spicy` (HotAPI - Z-Image Spicy)
    - `seedream-5.0-lite-spicy` (HotAPI - SeeDream 5.0 Lite Spicy)
    - `seedream-5.0-pro-spicy` (HotAPI - SeeDream 5.0 Pro Spicy)
    - `qwen-image-edit-spicy` (HotAPI - Qwen Image Edit Spicy)
    - `face-swap-spicy` (HotAPI - Face Swap Spicy: 元顔画像指定用エキスパートパラメータ `face_image` 付き)
  * **属性タグとモデルフィルタ**: 全HotAPIモデルに `NSFW対応`（`TAG_NSFW`）タグを付与し、モデルカタログ画面のタグフィルタで絞り込めるよう構成。
  * **APIキー・残高管理**: 設定画面（`SettingsPanel`）に `HotAPI Key (HOTAPI_KEY)` 入力欄および概算残高リセット設定を追加。`.env` および SQLite/`settings.json` へ双方向同期。
  * **単体テスト**: `tests/test_hotapi_provider.py` を追加し、モデル登録、モック生成、タスク送信・ポーリングシミュレーションの安全性を検証。

### 🚀 v4.7.7 (2026-07-25)
* **分類**: 総合バグ修正・安定性向上 (0.0.1アップ)
* **概要**: アプリ全体のクラッシュ経路、並行処理、永続化、画像読み込み、API入力、UI操作を再監査し、発見した不具合を包括的に修正。
* **詳細内容**:
  * **安全な終了処理**: 生成中はバックグラウンド処理が完了または安全に一時停止するまでQtウィンドウの破棄を待つよう変更し、終了時のスレッド競合による強制終了を防止。Python例外とネイティブ障害の診断ログも追加。
  * **単一起動とキュー整合性**: 多重起動を防止し、ジョブ取得をSQLite上のアトミック処理へ変更。失敗ジョブの誤完了、実行中ジョブの削除、途中バッチ再開時の重複生成、削除済みプロジェクトへの履歴保存失敗を修正。
  * **永続化の耐障害性**: 設定・APIキー・テンプレートをアトミック保存へ変更し、SQLite接続を例外時にも確実に解放。画像削除ではDB失敗時にファイルを復元し、DB登録に失敗した生成物は次回起動時に履歴へ回収。
  * **画像・UIのクラッシュ防止**: 巨大または破損した画像を縮小デコードし、異常な生成寸法を事前拒否。ギャラリー更新と一括削除の再入を防止し、選択状態・外部ビューア起動・空モデル一覧を安全に処理。
  * **API・モデル設定の修正**: 存在しないモデルへの暗黙フォールバックを廃止し、編集元画像欠落時の通常生成化を防止。fal.ai編集エンドポイントの複数画像入力形式、APIキー削除後の環境変数残留、OpenAI品質指定の上書き、品質値の変換を修正。
  * **テスト安全性**: 通常テストを一時データベースとモック生成へ隔離し、明示指定がない限り課金APIを呼ばない構成へ変更。キュー、履歴、削除復元、並行設定保存、API入力、画像形式、テンプレート移行、GUI起動の回帰テストを追加。

### 🚀 v4.7.6 (2026-07-24)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: Galleryパネルで画像のサムネイルをダブルクリックした際にOS標準の画像ビューアで開かなくなっていた問題を修正。
* **詳細内容**:
  * **サムネイルカードのマウスイベント透過処理**: `ThumbnailCard` 内の画像ラベル（`self.thumb`）およびタイトルラベル（`self.title_lbl`）に `Qt.WA_TransparentForMouseEvents` 属性を設定しました。これにより、子ラベルがダブルクリックイベントを吸収してしまい親フレーム (`ThumbnailCard.mouseDoubleClickEvent`) に届かなくなっていた問題を根本解消しました。
  * **右側プレビューのダブルクリック対応**: Gallery右側の拡大プレビュー表示枠 (`preview_lbl`) にも `ClickableLabel` を適用し、プレビュー画像をダブルクリックした際にもOS標準ビューアで直接画像を開けるように拡張しました。

### 🚀 v4.7.5 (2026-07-24)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: アプリ起動時に過去の未完了・残存ジョブが自動的にプロンプト精製・画像生成を開始してしまう不具合を修正。
* **詳細内容**:
  * **起動時ジョブ状態の自動一時停止**: アプリ起動時（`recover_interrupted_jobs`）に、過去セッションのデータベース上に残存していた `Running` または `Pending` 状態のジョブを、自動実行される `Pending` ではなく `Paused` 状態に切り替えるよう変更しました。
  * **Job Queue Manager への再開機能追加**: 「⏳ Job Queue」パネルに「▶️ Resume / Run Selected Job」ボタンを追加し、過去のジョブや一時停止中のジョブをユーザーの明示的な操作によって安全に再開（`Pending` 化）できるようにしました。

### 🚀 v4.3.0 (2026-07-22)
* **分類**: 機能統合・UI刷新 (0.1.0アップ)
* **概要**: Simple / Expert モード切替機能を廃止し、操作構造およびパラメータ読み書き処理を一本化。
* **詳細内容**:
  * **モードトグルの完全撤去**: ヘッダーの Simple / Expert モード切替ボタンおよび `ui_mode_changed` シグナル・関連設定を削除しました。
  * **操作構造の一本化**: AI Model ドロップダウンの選択と連動し、各モデル固有のエキスパートパラメータ領域（`ExpertModelPanel`）を常時表示・統合しました。
  * **パラメータ読み書き処理の一元化**: コード内の `current_ui_mode` や `isVisible()` などのモード判定フラグを完全廃止。生成リクエスト時・リテイク/コンテキスト読み込み時に、常にすべてのモデル固有パラメータを正しく取得・復元・API送信する堅牢な仕組みへ統一しました。

### 🚀 v4.2.1 (2026-07-22)
* **分類**: UI・機能改善 (0.0.1アップ)
* **概要**: ドラッグ＆ドロップ時にVision解析やEditモードへの自動切り替えを行わず、参照画像としての追加・表示のみにする改善。
* **詳細内容**:
  * **ドラッグ＆ドロップ動作の変更**: プレビューエリアに画像をドラッグ＆ドロップした際、自動的なVision解析（`VisionWorker`）やプロンプトの上書き、編集モード（Edit Mode）への強制変更を廃止しました。
  * **参照画像としての安全なセット**: ドロップされた画像はプレビューパネルに「参照画像」としてセット・表示され、ステータスバーに設定通知が表示されるのみとなります。ユーザーが意図して編集モードを選択した際に参照画像として利用できます。

### 🚀 v4.2.0 (2026-07-21)
* **分類**: 機能改善 (0.1.0アップ)
* **概要**: Galleryで画像を閲覧・選択しても、作業中の生成状態を変更しない非破壊な操作へ変更。
* **詳細内容**:
  * **選択と読込の責務分離**: Galleryのカード選択は、選択ハイライト、Gallery内の拡大プレビュー、メタデータ詳細表示だけを更新します。
  * **明示操作による反映**: 「コンテキストを読み込む（入力に反映）」を押した場合に限り、保存済み画像のプロンプト、モデル、サイズ、品質、スタイルプリセット、Expert設定をGeneration画面へ復元し、生成サービスの継続コンテキストも同期します。
  * **回帰防止**: 選択だけではコンテキスト読込要求が発生しないことと、明示操作では生成サービスおよびUIへ反映されることを自動テストで固定しました。

### 🚀 v4.1.7 (2026-07-19)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: モデル切り替え時に `expert_tab_widget` のカレントインデックスが同期されず、誤ったモデルの設定（またはデフォルト値）が送信されるバグを修正。
* **詳細内容**:
  * **タブ切り替え同期処理の追加**: `PromptPanel.on_model_changed` にて、モデル選択メニュー（`model_menu`）のインデックス変更を検知した際、`expert_tab_widget` 側のカレントインデックスも自動的に同じモデルのタブへ切り替える（同期する）処理を実装しました。これにより、手動切り替え・コードからの切り替えを問わず、常にUIの表示内容と実際にAPIへ送信されるパラメータの参照オブジェクトが正しく一致するようになりました。

### 🚀 v4.1.6 (2026-07-19)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: リテイクやLoad Context時に、モデル個別パラメータがUIのウィジェットに復元されず、UIの表示状態とAPI送信値が不一致になるバグを修正。
* **詳細内容**:
  * **モデルIDおよび個別設定のUI復元処理の実装**: ギャラリー等から過去のコンテキストをロード（`context_changed`）した際、モデルID自体の復元処理を追加するとともに、`expert_params` が保持する個別パラメータ（Seed, Safety Tolerance, Safety Checker, Output Formatなど）の値（コンボボックスの選択値やテキストエディタの入力）を、現在のアクティブな `ExpertModelPanel` に対して完全に書き戻し・復元する機能（`set_expert_params`）を実装しました。これにより、UIの見た目の設定値と実際にAPIに送信される設定値が常に完全に同期するようになりました。

### 🚀 v4.1.5 (2026-07-19)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: エキスパートモード有効時、縦解像度制限によって個別パラメータ領域（タブ部分）が完全に潰れて見えなくなるバグを修正。
* **詳細内容**:
  * **個別パラメータの最小高さ保証**: ウィンドウ全体の縦幅が制限された環境（標準フルHDモニター等）で起動・リサイズされた際、レイアウトエンジンの優先度によって `expert_tab_widget`（モデル個別オプション設定欄）の高さが完全に `0`（タブバーのみの高さ）まで潰され、パラメータが見えなくなるバグを修正しました。最小高さ制限（`setMinimumHeight(210)`）を明示的に設けることで、どのようなウィンドウ伸縮時にもパラメータエリアの十分な視認性と操作性を保証するようにしました。

### 🚀 v4.1.4 (2026-07-19)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: ツール起動時におけるUIモードトグルボタンと実際のUIレイアウトの不整合バグを修正。
* **詳細内容**:
  * **初期UIモードのグローバル同期**: 保存されているUIモード（`ui_mode`）の初期設定値に関わらず、アプリ起動時に EventBus を通じて `ui_mode_changed` シグナルを送信する処理を追加しました。これにより、ヘッダーのトグルボタンと `PromptPanel` などの個別パネルとの間で、初期状態（トグルボタンのチェック状態および表示・非表示のレイアウト）が完全に同期されるようになり、表示の食い違いが発生しなくなりました。

### 🚀 v4.1.3 (2026-07-19)
* **分類**: リファクタリング (0.0.1アップ)
* **概要**: 静的解析（Ruff/Mypy）によるデッドコード、型不整合、未使用変数、PySide6警告等のクリーンアップ。
* **詳細内容**:
  * **デッドコード・未使用インポートの自動クリーンアップ**: `ruff check . --fix` を用いて、未使用のライブラリインポートや重複記述、冗長な条件比較など計32件を完全に除去・自動修正。
  * **PEP 484準拠の型定義に修正**: 引数のデフォルト値に `None` を取る場合に明示的に `Optional`（`str | None` など）でアノテーションしていない箇所について、厳格な静的型チェックに対応。
  * **型不整合の修正**: `fal_provider.py` においてAPIパラメータ用辞書が文字列用 (`dict[str, str]`) と誤認識される不具合を `dict[str, Any]` に明示。推定コストの取得時に浮動小数点数型への安全なキャスト (isinstanceチェック) を追加。
  * **PySide6警告の修正**: 画像サイズ変換処理で非推奨・無効となる `Qt.KeepAspectRatio` などの呼び出しを最新の `Qt.AspectRatioMode.KeepAspectRatio` へと更新。
  * **Noneクラッシュ防止ガードの追加**: 翻訳結果文字列が万が一 `None` だった場合に `.strip()` を呼んでクラッシュするのを防ぐため、値の存在チェックガードを追加。

### 🚀 v4.1.2 (2026-07-19)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: FLUXモデルにおける Safety Tolerance の上限値（6）の削除およびデフォルトの5への変更。
* **詳細内容**:
  * **不正パラメータの削除**: fal.ai 側のFLUXモデルのAPIでは `safety_tolerance` の許容範囲が `1` から `5` までとなっていますが、定義ファイル上で `6 (Permissive)` まで選択可能になっており、これを選んで生成するとAPIエラーになる不具合がありました。UIから選択肢 `6` を削除し、選択肢を `1`〜`5` とすると同時に、デフォルト値を `5` に変更しました。

### 🚀 v4.1.1 (2026-07-19)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: Expert Modeのパラメータ送信時、UIレイアウトの状態により値が正しく送信されないバグの修正。
* **詳細内容**:
  * **パラメータ取得条件の改善**: 画面下部の「Generate」ボタン押下時など、UIのレイアウト再計算が走るタイミングで `expert_tab_widget.isVisible()` が一時的に `False` と判定され、設定したExpertパラメータ（Safety CheckerやSafety Tolerance等）が `None`（空）としてAPIへ送信されてしまう不具合がありました。判定ロジックを、描画状況に依存する `isVisible()` から、保持されているUIモード（`self.current_ui_mode == "expert"`）による判定へと変更し、常に設定値が反映されるように修正しました。

### 🚀 v4.1.0 (2026-07-18)
* **分類**: 機能追加 (0.1.0アップ)
* **概要**: 生成操作を常時アクセス可能にし、キューの処理状況とプレビュー後の再生成操作を明確化。
* **詳細内容**:
  * **固定生成・進捗バー**: Generation画面下部に `Generate`、実行中ジョブ番号、待機件数、経過時間を集約しました。待機中の先頭ジョブを取り消す操作も追加しています。実行中のAPIリクエストは安全に中断できないため、取消対象を待機中ジョブに限定しています。
  * **プレビュー操作の明確化**: 「再生成」は現在入力中の設定を使い、「設定を復元して再生成」は画像保存時のプロンプト、モデル、Expert設定などを復元して実行します。
  * **撤去しやすい構成**: プレビューの任意操作は `PreviewPanel.OPTIONAL_ACTIONS`、下部バーは `GenerationActionBar` に分離しました。利用価値の検証後、該当する要素を局所的に削除できます。
  * **バージョン表記の統一**: `core/version.py` を唯一のバージョン定義とし、ウィンドウタイトルと起動ログで共通利用します。

### 🚀 v4.0.3 (2026-07-18)
* **分類**: バグ修正・調整 (0.0.1アップ)
* **概要**: Expert Modeパラメーターの欠落バグ修正と初期ウィンドウサイズの調整。
* **詳細内容**:
  * **初期ウィンドウサイズの調整**: Expert Modeのパラメータ入力欄が隠れないよう、初期ウィンドウサイズを 1100x1100 から 1200x1150 へ拡大しました。
  * **データパイプラインの修正**: UIで設定した `expert_params` がキューへ登録される際に消失していたバグを修正。同時に履歴データベース（SQLite）の保存および読み込みロジックにも `expert_params` を追加し、正しく保存・表示されるように修正しました。
  * **リテイク時のパラメーター復元**: ギャラリーやプレビューからのリテイク（再生成）時にも、元の画像に使われていた `expert_params` が正しく引き継がれるように修正しました。
  * **耐障害性の向上**: プロバイダー側でのパラメータパース処理において、予期せぬ入力があった場合でも全体の処理がクラッシュせず、正常なパラメータのみを適用するように例外処理を強化しました。

### 🚀 v4.0.2 (2026-07-18)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: メタデータ保存時のStylePreset反映漏れ修正、および GUI パラメータの登録修正。
* **詳細内容**:
  * **StylePreset のメタデータ反映**: バックエンド（`GenerationService`）において、APIへ送信する最終プロンプト（`prompt_en_final`）には正しく StylePreset のキーワードが結合されていたものの、メタデータ出力（JSONおよびSQLite DB記録時）には結合前の変数（`prompt_en`）が使用されてしまっていたため、UIのメタデータダンプで効果が見えない状態でした。これを最終結合後のプロンプトを記録・表示するように修正しました。
  * **Safety Checker GUI表示の修正**: 前回のアップデートで `enable_safety_checker` オプションのモデルレジストリ登録処理が一部書き込みエラーによりスキップされていたためUIに反映されていませんでした。これを再登録し、正しくExpert Modeのタブ内に表示されるよう修正しました。

### 🚀 v4.0.1 (2026-07-18)
* **分類**: 機能追加・バグ修正 (0.0.1アップ)
* **概要**: FLUXモデルの Safety Checker オプション追加およびパラメータクラッシュのバグ修正。
* **詳細内容**:
  * **Enable Safety Checker オプションの追加**: FLUXモデル（fal.ai の FLUX.2 Pro および Dev）の Expert Mode に `enable_safety_checker` オプションを追加しました。デフォルト値はご要望通り `Off` に設定されています。
  * **旧ロジックの廃止**: `quality` パラメータが `High` の場合にハードコードで Safety Checker を強制的に True にしていた古いフォールバックロジックを削除し、Expert Mode の設定値が完全に優先されるように修正しました。
  * **空パラメータ時のバグ修正**: Expert Mode で数値を空欄のまま送信した際に発生する `TypeError` クラッシュを防止する安全処理をプロバイダークラスに実装しました。

### 🚀 v4.0.0 (2026-07-17)
* **分類**: メジャーアップデート (1.0.0アップ)
* **概要**: UIの大幅なバージョンアップ (Simple / Expert Mode の導入と動的パラメータUIの追加)
* **詳細内容**:
  * **Simple/Expert Mode のトグル**: 画面最上部（ヘッダー領域）に Simple Mode と Expert Mode を切り替えるトグルボタンを新設。ユーザーのモード選択状態は `settings.json` に保存され、次回起動時にも復元されます。
  * **Expert Mode UI**: Expert Mode 選択時、従来の単一AIモデル選択コンボボックスが、有効化された各AIモデルごとの「モデルタブ（`ExpertModelPanel`）」に切り替わります。
  * **動的パラメータ生成**: `model_registry.py` に各AIモデル（FLUX, Grok, GPT Image, Qwen, Seedream 等）固有の `expert_params` スキーマ定義を追加。この定義に基づいて Expert Mode タブ内に固有のパラメータ入力UI（コンボボックス、テキスト入力等）が動的に構築されます。
  * **バックエンド・DB連携**: データベースの `jobs` テーブルおよび `images` テーブルに `expert_params` カラム（JSON文字列）を追加し、ジョブキューから画像生成API (`fal_provider` 等) へパラメータが正確に引き渡されて合成されるよう処理パイプライン全体を拡張しました。

### 🚀 v3.5.0 (2026-07-17)
* **分類**: 機能追加 (0.1アップ)
* **概要**: StylePreset（スタイルプリセット）機能の復活および自動プロンプト挿入の実装
* **詳細内容**:
  * **StylePreset の復活**: 以前削除されたスタイルプリセット機能を復活させました。初期値は空とし、ユーザーが自由に定義して適用できます。
  * **プロンプトへの自動結合（バックエンド処理）**: 選択されたスタイルプリセットは、翻訳が完了した英語プロンプトの先頭に自動でカンマ結合されてAPIへ送信されます。UIの入力欄には表示されないため、プロンプト欄が散らかるのを防げます。
  * **UIの刷新と管理画面の統合**: `PromptPanel` に `Style Preset:` の選択コンボボックスと 「⚙️ Manage Presets」ボタンを追加（Prompt Templateの上に配置）しました。「⚙️ Manage Presets」をクリックすると、既存のテンプレート管理ダイアログ（`TemplateDialog`）に新設された「Style Preset」タブがアクティブになった状態で開き、追加・編集・削除が行えます。また、追加時のグリッド行インデックスの重複により、下部のクオリティと生成枚数（Batch Count）のラベルテキストが重なって描画されてしまうレイアウトバグを修正しました。
  * **データベース・メタデータ・キュー連携**: `jobs` および `images` テーブルに `style_preset` カラムを追加。JSONメタデータ、履歴からのロード（Load Context）、およびリテイク時にも正確に選択中のプリセット状態が復元されるようにデータベースを拡張しました。

### 🚀 v3.4.1 (2026-07-17)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: 画像リテイク/編集時における NameError の修正
* **詳細内容**:
  * `openai_translator.py` の `translate_modified_prompt` メソッド内で、以前のハードコードされた `style` 変数のチェック処理が残存していたため、プロンプト編集・リテイク処理を行う際に `NameError: name 'style' is not defined` エラーが発生して生成に失敗する問題を解消しました。
  * `translate_prompt` と同様に、動的ルール読み込み処理 (`load_translation_rule`) に置き換えを行いました。

### 🚀 v3.4.0 (2026-07-17)
* **分類**: 機能追加 (0.1アップ)
* **概要**: Translation Rule への名称変更およびルールの外部ファイル化
* **変更点**:
  * **名称変更**: "Style Engine" という名称を "Translation Rule" に全面的に変更しました。関連する変数名やUIラベル、プレビュー表示も更新しています。
  * **外部ファイル化**: ハードコードされていたプロンプト翻訳ルールを廃止し、`translation_rules` フォルダ内の `.md` ファイルから動的に読み込むアーキテクチャに変更しました。これにより再起動するだけで新しい翻訳ルールを簡単に追加・変更できるようになりました。
  * **UI変更**: コンボボックス（ドロップダウン）形式だったルール選択UIを、ラジオボタンの横並びレイアウトに変更し、一覧性と選択のしやすさを向上させました。
  * **既存ルールの整理**: 使用頻度の低い3つのルール（Photo - Studio, Photo - Snap, Anime - Cinematic）を削除し、`Standard` およびパススルー用の `Raw - Direct` のみに整理しました。

### 🚀 v3.2.0 (2026-07-15)
* **分類**: 機能追加 (0.1アップ)
* **概要**: StylePreset と Prompt Template の統合および UI/ロジックの整理
* **変更点**:
  * **データ統合**: `presets.json` を `prompt_templates.json` (またはデータベースのテンプレート領域) に移行・マージし、以後は Prompt Template として一元管理する仕組みに変更。古い `presets.json` は移行後に自動削除されます。
  * **UIの整理**: `PromptPanel` から Style Preset メニューを削除し、Template 系メニューの行を上に詰めました。古い `style_preset_dialog.py` を削除しました。
  * **ロジック整理**: 翻訳ロジック（OpenAI, Gemini）および画像生成・キュー処理（`generation_service`, `queue_service`, `main_window`）の全般から、使用されなくなった `style` 引数・処理を削除し、コードをクリーンアップしました。データベースの互換性のため履歴テーブル側のカラムは維持しつつ、空データを保存するように調整しています。

### 🚀 v3.1.7 (2026-07-15)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: OpenAIの一部特定モデル（gpt-image-2など）におけるQuality値送信エラーの修正。
* **詳細内容**:
  * **API Quality値のインテリジェントマッピング**:
    * 独自エンドポイント `gpt-image-2` などのOpenAIモデルにおいて、DALL-E 3用の `hd` / `standard` をパラメータで送信すると「`Invalid value: 'hd'. Supported values are: 'low', 'medium', 'high', and 'auto'.`」という 400 Bad Request エラーが発生する問題を解決しました。
    * APIクライアント内部でモデル名を判別し、モデル名に `gpt-image` が含まれる場合は高品質指定（High Quality）を `high` に、標準指定（Standard）を `medium` にマッピングして送信するように改修。DALL-E 3に対しては引き続き `hd` / `standard` で送信されます。
* **対象コミット/対話**: 対話 77eb5918

### 🚀 v3.1.6 (2026-07-15)
* **分類**: 機能改善・UI調整 (0.0.1アップ)
* **概要**: クオリティ選択機能（Quality）のOpenAIモデル専用化およびUI仕様の変更。
* **詳細内容**:
  * **OpenAIモデル専用化とグレーアウト制御**:
    * 選択したAI Modelのプロバイダーが `openai` の場合のみクオリティ設定を選択可能にし、それ以外のプロバイダー（fal.ai, Grok等）のモデルが選択されている場合は品質設定エリア（ラベルおよびラジオボタン）をグレーアウトして無効化する制御を追加しました。
  * **選択項目の整理とAPI値のマッピング**:
    * ラベル名を「Quality (OpenAI):」に変更。
    * 従来の3段階（Low/Medium/High）から、OpenAI API仕様に適合する「Standard」（内部値 `standard`）と「High Quality」（内部値 `hd`）の2段階のラジオボタンに整理しました。
    * APIクライアント側のコスト計算やDALL-E 3パラメーターマッピングも `hd` / `standard` に対応するよう調整しました。
* **対象コミット/対話**: 対話 77eb5918

### 🚀 v3.1.5 (2026-07-15)
* **分類**: バグ修正 (0.0.1アップ)
* **概要**: UIコンポーネントにおける状態リセットバグおよびコンテキスト反映バグの修正。
* **詳細内容**:
  * **モデル選択状態の維持**:
    * 画像生成の完了毎にGeneration ParametersのAI Modelドロップダウンがリストの先頭にリセットされてしまう問題について、メニュー再構築時に前回の選択状態を記憶・復元する処理（`currentData()`の退避と再適用）を追加して修正しました。
  * **Gallery Load Contextの修正**:
    * Galleryタブでサムネイルを選択して「Load Context」ボタンを押下した際、シグナルの競合（タブ切り替えの空ペイロードによる上書き）が発生してプロンプト入力欄が空になってしまうバグを修正。ディクショナリを統合することで確実に入力欄へ反映されるようになりました。
* **対象コミット/対話**: 対話 77eb5918

### 🚀 v3.1.4 (2026-07-15)
* **分類**: バグ修正・クリーンアップ (0.0.1アップ)
* **概要**: Opus実行計画に基づく包括的検証レポートの作成、および特定されたクリティカルバグやデッドコードの完全な修正。
* **詳細内容**:
  * **クリティカルバグの修正**:
    * **PreviewPanelリテイクバグ**: 「Direct Retake」実行時に `model_id` と `image_path`（編集モード時）が欠落し、常に OpenAI の text-to-image で生成されてしまうバグを修正。UI状態およびモデルのカテゴリ（`img_edit`）を判定し、正確に元のモードとモデルでリテイクされるよう堅牢化。
    * **fal.ai マスクバグ**: Qwen 2.0 Edit などの特定モデルで、エンドポイント名に `"fill"` が含まれていないためにマスクが API へ送信されず無視されるバグを修正。すべての編集系モデルで安全にマスクが送信されるよう条件を緩和。
  * **デザイン・ロジックの改善**:
    * OpenAI（DALL-E）やその他のモデルで、編集が `generate` にフォールバックしたりモック実行されたりする際に、ユーザーが選択した Quality（Low/Medium/High）パラメータが `"Medium"` にハードコード固定されてしまう問題を、全プロバイダーとインターフェースのシグネチャを改修して解決。
    * ステータスバーの残高表示が `$10.00000` のように長くなりすぎる問題を、2桁小数（`$10.00`）へフォーマット調整。
    * Tips タブでの fal.ai のプロバイダー表記ゆれ（`FAL`）を `fal.ai` に統一。
  * **デッドコード・未使用importのクリーンアップ**:
    * プロジェクトから完全に切り離されていた `api/edit_client.py` を削除。
    * `main_window.py`, `gallery_panel.py`, `preview_panel.py`, `event_bus.py`, `openai_provider.py` に残存していた未使用の `import` 文やデッドシグナル、未使用のインスタンス変数をクリーンアップし、パフォーマンスと可読性を向上。
* **対象コミット/対話**: 対話 77eb5918


### 🚀 v3.1.3 (2026-07-14)
* **分類**: レイアウト・UIスタイリングの調整 (0.0.1アップ)
* **概要**: Galleryタブの選択プレビュー画像サイズを Generation プレビューと統一。
* **詳細内容**:
  * **プレビュー画像の固定サイズ化**:
    * Galleryパネルの右側詳細欄に表示される拡大プレビュー画像のサイズを、Generationタブのプレビューと同じ `450x450` ピクセル固定に設定。
  * **アライメントの調整**:
    * 拡大プレビュー画像が詳細領域の横幅に対して中央に配置されるように `Qt.AlignCenter` を適用。
* **対象コミット/対話**: 対話 d0ceba1c

### 🚀 v3.1.2 (2026-07-14)
* **分類**: レイアウト・UIスタイリングの調整 (0.0.1アップ)
* **概要**: PreviewPanel内の要素レイアウト最適化。ボタン群を画像直下へ移動し、詳細ダンプ欄を縦に拡大。
* **詳細内容**:
  * **アクションボタン位置の移動**:
    * これまで中央付近に浮いていた3つのアクションボタン（⭐ Mark Favorite, 🔄 Direct Retake, 📁 Open Folder）を、プレビュー画像フレームのすぐ直下に密着させて再配置。
  * **メタデータログ表示枠の縦幅拡大**:
    * 固定されていた `120px` の高さ制限を撤廃し、縦に自由伸縮可能なストレッチ設定（stretch factor 1）に変更。
    * これにより、ボタンの下から画面下部までの空き領域全体をカバーする形で表示枠が縦方向に大きく拡張され、長いプロンプトなどの詳細情報がスクロールなしで見やすくなりました。
* **対象コミット/対話**: 対話 77eb5918

### 🚀 v3.1.1 (2026-07-14)
* **分類**: レイアウト・UIスタイリングの調整 (0.0.1アップ)
* **概要**: Galleryタブのスプリットレイアウト調整およびダンプ上部への拡大プレビュー表示機能の追加。
* **詳細内容**:
  * **スプリットレイアウト比率の調整**:
    * Gallery内のサムネイルグリッド側の両余白を極限まで減らし、右側の詳細情報表示エリア（Metadata Log Detail Dump）の横幅を大きく拡張。
  * **選択画像の拡大プレビュー表示**:
    * 選択したサムネイル画像の拡大プレビューを詳細ダンプテキストの上部に動的配置。
    * ウィンドウのリサイズ時にもアスペクト比を維持したまま、ラベル幅に合わせて拡大画像がスムーズに再スケーリングされる追従機能を実装。
* **対象コミット/対話**: 対話 77eb5918

### 🚀 v3.1.0 (2026-07-14)
* **分類**: 機能追加 (0.1.0アップ)
* **概要**: fal.aiのモデル刷新、個別残高シミュレーターの実装、Tipsタブの追加、およびウィンドウ配置最適化。
* **詳細内容**:
  * **fal.ai モデルの構成刷新**:
    * 旧FLUX.1系列を削除し、最新の `FLUX.2 Pro/Dev` を追加。
    * ByteDance `Seedream 5.0 Pro (生成/編集)`、Alibaba `Qwen Image 2.0 (生成/編集)`、OpenAI `GPT Image 2` の fal.ai 経由エンドポイントを追加。
    * Grok Imagine モデルに高品質版 `xai/grok-imagine-image/quality/text-to-image` を追加。
  * **3プロバイダー個別残高管理**:
    * OpenAI、fal.ai、Grok (xAI) の消費残高を別々に管理するローカルシミュレーターへ拡張。
    * Settings画面にて各プロバイダー別に初期残高のチャージ（リセット）が可能。
    * ステータスバー右側に「OpenAI: $X.XX | fal.ai: $Y.YY | Grok: $Z.ZZ」の形式で常時並列表示。
  * **「💡 Tips」タブの新規追加**:
    * 登録されている各モデルの対応機能や推定コストを、高い順にソートしてプレミアムテーブル形式で一覧表示。
    * 各AIモデルの得意分野・得意とするタッチなどの特徴解説や、美しい画像を生成するためのプロンプトテクニックガイドを掲載。
  * **初期ウィンドウサイズの拡大**:
    * UIのボタンや各種パラメーター入力欄が重ならずにアライメントが正しく保持されるよう、初期ウィンドウサイズを `1280x920` (最小 `1100x800`) に拡大。
* **対象コミット/対話**: 対話 77eb5918

### 🚀 v3.0.0 (2026-07-14)
* **分類**: ソースコード全体の書き直し / 大規模機能追加 (1.0.0アップ)
* **概要**: 「AI Image Studio」への改称、および複数プロバイダー（OpenAI, fal.ai, Grok Imagine）とGemini翻訳エンジンへの大規模リファクタリング。
* **詳細内容**:
  * **アプリの改称**: マルチプロバイダー化に伴い、名称を「AI Image Studio」に変更。UIヘッダーやタイトルを統一。
  * **マルチプロバイダー画像生成/編集**:
    * OpenAI（`gpt-image-2`）に加え、`fal.ai` (FLUX.1 [dev/schnell/pro], Kontext, Recraft V4 Pro) と `Grok Imagine` (xAI: grok-imagine-image-quality / standard) をサポート。
    * プロバイダー/クライアント構造を抽象化・疎結合化し、`MODEL_REGISTRY` への設定定義の追加とプロバイダークラス実装のみで容易に新モデル・エンジンを動的追加・変更可能な設計へリファクタリング。
    * ジョブキュー（QueueService）でもモデル指定および画像編集（Edit/Inpaint）ジョブがバックグラウンド実行できるようにスキーマおよびスレッドコンシューマーを拡張。
  * **Gemini プロンプト翻訳**:
    * 翻訳エンジンに Google の最新 SDK `google-genai` を使った `gemini-3.1-flash-lite` を追加。
    * Settings画面にて OpenAI GPT と Gemini の切り替えが可能。プロンプト拡張・安全バイパス・ネガティブ要素の指示ロジックは既存の OpenAI 翻訳を踏襲。
  * **モード切替トグルおよびモデル連動**:
    * メイン画面に明示的な「🎨 生成モード」と「✏️ 編集モード」のトグルボタンを新設。
    * 選択したモードに応じて、Settingsでユーザー自身が有効化したモデルだけがAI Modelドロップダウンに動的フィルタ表示されるよう連動。
    * モデル変更時に、サポートされるアスペクト比/サイズリストがアスペクト比選択メニューへ動的切り替えされるよう実装。
    * 編集モードでソース画像が選択/ドロップされていない場合、「⚠️ ソース画像を指定してください」と警告するヒントラベルを表示して誤操作を防止。
  * **データベースマイグレーション**: 既存DBとの後方互換性を保つため、`images` テーブルに `model_name`, `provider` カラム、`jobs` テーブルに `model_id`, `provider`, `mode`, `image_path`, `mask_path` カラムを動的追加する自動マイグレーション処理を実装。
* **対象コミット/対話**: 対話 77eb5918

### 🔧 v2.5.2 (2026-07-14)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: Galleryタブ上部の余白削除とレイアウトの最適化。
* **詳細内容**:
  * **上部ツールバーの完全削除**: Galleryパネル上部にあった「Favorites Only」などのツールバー枠を削除し、縦方向に余計なスペースを占有していた無駄な余白を完全に排除しました。
  * **ボタンの移動**: 一括操作に必要な「Select All」および「Clear Selection」ボタンを、下部の「Bulk Export Selected」などのアクションバー（Bulk Actions Toolbar）に統合・移動し、スマートなレイアウトに変更しました。
  * **バージョン表記の統一**: `app.py` および `ui/main_window.py` のバージョン表記を `Ver2.5.2` に更新しました。
* **対象コミット/対話**: 対話 8f90aa7a

### 🔧 v2.5.1 (2026-07-14)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: Galleryタブの検索バー削除。
* **詳細内容**:
  * **検索入力の削除**: Gallery上部ツールバーから「Filter gallery items... (リアルタイム検索)」テキスト入力欄を不要のため削除しました。
  * **バージョン表記の統一**: `app.py` および `ui/main_window.py` のバージョン表記を `Ver2.5.1` に更新しました。
* **対象コミット/対話**: 対話 8f90aa7a

### 🚀 v2.5.0 (2026-07-14)
* **分類**: 機能追加 (0.1アップ)
* **概要**: GalleryタブとHistoryタブの統合による画像・履歴管理の効率化。
* **詳細内容**:
  * **タブの統合**: 従来の `History` タブを廃止し、`Gallery` タブへと機能を統合しました。
  * **レイアウト変更 (QSplitterの導入)**: `Gallery` 内に水平スプリッターを導入し、左側にサムネイルグリッド（従来の4列から3列へ変更）、右側に「Metadata Log Dump」詳細表示エリア（読取専用 `QTextEdit`）および「Load Context (入力に反映)」ボタンを配置しました。
  * **Favorites Only お気に入りフィルタの統合**: ギャラリー上部のツールバーに「Favorites Only」チェックボックスを追加し、お気に入り画像のみを瞬時にフィルタリングできるようにしました。
  * **コンテキスト自動復元とタブ遷移**: ギャラリーの画像をクリックすると右側のダンプ欄にメタデータがリアルタイム表示され、「Load Context」ボタンをクリックすると対応するプロンプトや設定（品質、サイズ等）が生成コントロールに反映され、かつ自動的に `Generation` タブへ切り替わるよう連携しました。
  * **不要コードのクリーンアップ**: 統合に伴い不要になった `history_panel.py` を削除しました。
  * **バージョン表記の統一**: `app.py` および `ui/main_window.py` のバージョン表記を `Ver2.5.0` に更新しました。
* **対象コミット/対話**: 対話 8f90aa7a

### 🔧 v2.4.5 (2026-07-14)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: テンプレート管理ダイアログの起動エラー修正。
* **詳細内容**:
  * **インポートエラーの解消**: `TemplateDialog` 内部で `QWidget` が使用されているにもかかわらず `PySide6.QtWidgets` からインポートされていなかったため、クリック時に `NameError: name 'QWidget' is not defined` が発生する問題を修正。
  * **バージョン表記の統一**: `app.py` および `ui/main_window.py` のバージョン表記を `Ver2.4.5` に更新。
* **対象コミット/対話**: 対話 8f90aa7a

### 🔧 v2.4.4 (2026-07-14)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: 配布パッケージの軽量化およびセットアップ開始前のユーザー同意確認（Y/N）の実装。
* **詳細内容**:
  * **仮想環境（.venv）の配布物除外**: 配布用パッケージから巨大な `.venv` ディレクトリを削除し、軽量化。
  * **インストール同意確認（Y/N）プロンプトの実装**: 実行環境に依存パッケージ（PySide6, openai など）がインストールされていない場合、自動セットアップを開始する前にユーザーへ Y/N で実行同意を求めるプロンプトを追加。
  * **無駄なプロンプトのスキップ**: すでにセットアップが完了している2回目以降の起動時は、確認プロンプトを出さずに瞬時にアプリを起動するように調整。
  * **バージョン表記の統一**: `app.py` および `ui/main_window.py` (開発元・配布用とも) のバージョン表記を `Ver2.4.4` に更新。
* **対象コミット/対話**: 対話 e914273e

### 🔧 v2.4.3 (2026-07-14)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: 配布用起動スクリプト (`start.bat`) の安定化、環境チェック強化、およびパッケージ自動インストール処理の最適化。
* **詳細内容**:
  * **カレントディレクトリの保証**: `start.bat` の先頭に `cd /d "%~dp0"` を追加し、管理者として実行された場合でもカレントディレクトリがバッチファイル所在位置になるよう保証。
  * **環境変数遅延評価の修正**: `if` 括弧内の `%errorlevel%` を `!errorlevel!` に切り替え、仮想環境作成エラー等の成否が正しく判定されるように修正。
  * **Python実行・バージョンチェックの強化**: `where python` だけでなく、実際に `python --version` や `python -c` などを実動作テストすることで、Windowsの「アプリ実行エイリアス」によるダミー Python を検出可能に。また Python 3.10 以上が動作しているか検証。
  * **アクティベート前のファイル存在確認**: 仮想環境作成失敗等による `call` エラーでの一瞬のバッチ強制終了を防ぐため、`activate.bat` の存在判定を挟むように改善。
  * **ライブラリ自動インストールの高速化**: 起動のたびに `pip install` が走るのを防ぐため、`python -c "import ..."` で必要な外部モジュール（`openai`, `PySide6`, `PIL`, `dotenv`, `requests`）が揃っているかを事前にテストし、不足時のみインストールを実行するロジックに最適化。
  * **バージョン表記の統一**: `app.py` (起動時ログ) および `ui/main_window.py` (ウィンドウタイトル) のバージョン表記を `Ver2.4.3` に更新。
* **対象コミット/対話**: 対話 e914273e

### 🔧 v2.4.2 (2026-07-13)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: Comparison タブの削除、および Batch Count スピンボックスの矢印が機能しないバグの修正。
* **詳細内容**:
  * **Comparisonタブの完全削除**: ユーザー要件（機能不全および不要）に基づき、MainWindow のタブ構成から `Comparison` タブを除去し、`compare_panel.py` および `zoomable_view.py` を完全にクリーンアップしました。
  * **スピンボックス矢印クリックバグの修正**: QSS の `padding: 4px 8px` 設定が QSpinBox のスピン矢印ボタンのクリック可能領域を覆ってしまい、矢印ボタンが反応しない問題を修正するため、QSpinBox に対してのみ右パディングを `20px` に広げるスタイルを追加しました。
  * **スピンボックスの幅アライメント調整**: `Batch Count` のスピンボックスが小さすぎて矢印が潰れないよう、グリッドレイアウト上の配置を3列スパン（col 1から3列分）に拡張しました。
  * **バージョン表示の修正**: アプリのバージョンを `Ver2.4.2` に更新しました。
* **対象コミット/対話**: 対話 177e3bd2

### 🔧 v2.4.1 (2026-07-13)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: Generation Parameters 内のテンプレート関係ウィジェットの配置（幅）をグリッドに直接配置して統一。
* **詳細内容**:
  * **ウィジェットアライメントの統一**: ドロップダウン（コンボボックス）やボタンを `QHBoxLayout` 経由で追加するのではなく、直接 `QGridLayout` (gen_grid) のカラムに配置し、列ストレッチ比（3:2:2）を定義することで、どの画面サイズや初期起動状態においても、すべての行のパーツ幅が完全に揃うように改善しました。
  * **関連項目のスパン表示化**: グリッドのカラム数が4に増えたため、Size Aspect Ratio や Quality などの他の項目は複数カラム（col 1から3列分）に跨るようにスパン設定を行い、見た目の整合性を維持しました。
  * **バージョン表示の修正**: アプリのバージョンを `Ver2.4.1` に更新しました。
* **対象コミット/対話**: 対話 177e3bd2

### 🚀 v2.4.0 (2026-07-13)
* **分類**: 機能追加 (0.1アップ)
* **概要**: StylePresetとTemplatesタブを統合し、メイン画面でのクイックなテンプレート適用を実現。
* **詳細内容**:
  * **Templatesタブの廃止と統合**: 従来の「Templates」タブを廃止し、メイン画面（`Generation` タブの `PromptPanel`）の `Generation Parameters` 内に機能を統合しました。
  * **プロンプト＆ネガティブテンプレートのクイック選択**: `Generation Parameters` 内に `Prompt Template` と `Negative Template` のドロップダウンメニューを追加しました。
  * **ワンクリック適用機能**: 選択したテンプレートの内容を、ボタンクリックで即座に Positive Prompt または Negative Prompt に挿入・適用できるようにしました。
  * **ネガティブプロンプトの複数組み合わせ適用**: ネガティブテンプレートを選択して適用する際、すでに入力がある場合は自動的にカンマ区切りで追記（`append_neg_tag`）する仕組みを導入し、複数のネガティブテンプレートを組み合わせて適用できるようにしました。
  * **テンプレート管理ダイアログの導入**: 新規追加・編集・削除をその場で行えるようにする「⚙️ Manage Templates」ボタンを追加し、ポップアップ形式の `TemplateDialog` を実装しました。ダイアログ起動時には自動的に選択された種類（プロンプト用／除外要素用）のタブがアクティブになります。
  * **自動同期の実装**: ダイアログ内でのテンプレートの保存・削除が完了すると、`template_updated` イベントを介してメイン画面のコンボボックス一覧が即座に同期・自動更新されるようにしました。
  * **バージョン表示の修正**: アプリのバージョンを `Ver2.4.0` に更新しました。
* **対象コミット/対話**: 対話 177e3bd2

### 🔧 v2.3.3 (2026-07-13)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: UI重なり・見切れの解消、および生成中のステータス点滅表示実装。
* **詳細内容**:
  * **アクションボタン重なりの解消**: プロンプト入力欄の高さを `145px` から `175px` に拡張し、ボタン of 縦合計高さと並びの余白を最適化することで、GenerateボタンとClear Contextボタンの重なりを完全に解消しました。
  * **ボタン内日本語表記の削除**: 不要な重なりやレイアウトの窮屈さを防ぐため、ボタンテキスト内の日本語表記（「(新規作成)」など）を削除し、`New Create`、`Generate`、`Clear Context` へとテキストをスリム化しました。
  * **文字見切れの解消**: Style Preset や Size Aspect Ratio などのコンボボックス、入力欄、ボタンの高さが足りずに文字の下部が見切れていた問題を解決するため、各ウィジェットの高さを明示的に `30px` に固定。さらに QSS の `padding` を `4px 8px` に縮小し `min-height: 24px` を定義して、文字が欠けずに綺麗に中央配置されるよう調整しました。
  * **生成中のステータス青字・点滅化**: 画像の生成処理（Mock生成を含む）が実行されている間、ステータスバーの左下メッセージを青太字の `"Now Generating"` に切り替え、`QTimer` を用いて 500ms 間隔で点滅（表示・非表示の切り替え）させることで、処理中であることが一目で伝わるようビジュアルを強化しました。
  * **ステータス点滅制御のバグ修正**: データベース上の過去レコードに依存せず、QueueConsumerスレッドのアクティブ状態（ Generating job メッセージの送信）に同期して点滅の開始・停止を切り替えるよう修正し、待機時の誤点滅を防ぎました。
  * **ウィンドウ初期サイズの微調整**: UI全体の収まりを改善し、プロンプト等のコントロールが完全に表示されるよう、初期起動時のウィンドウサイズを `1220x880` に、最小サイズを `1050x760` に拡大しました。
  * **バージョン表示の修正**: ウィンドウタイトルの表記を `Ver2.3.3` に更新しました。
* **対象コミット/対話**: 対話 bba790d6

### 🔧 v2.3.2 (2026-07-13)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: アクションボタンおよびネガティブプロンプトのレイアウト修正、Codexテーマの移植。
* **詳細内容**:
  * **レイアウトの完全修正（ボタン配置）**: プロンプト入力欄の右隣に3つの主要ボタン（New Create, Generate, Clear Context）が縦に綺麗に並び、ネガティブプロンプト入力欄はその下で横いっぱいに広がる正しい回り込みレイアウトを構築しました。
  * **プロンプト入力欄の高さ最適化**: 右側ボタン群の合計の高さ（182px）と並びが完璧に揃うよう、プロンプト入力欄の高さを `145px` に拡張し、視覚的なバランスと入力効率を向上させました。
  * **Codexテーマのインポート**: `main_window_codex.py` に記述されていたヘッダーフレーム構造（ブランドロゴ、サブタイトル、WORKSPACEバッジ）および詳細なプレミアムQSS（フォントファミリー、透過タブ、青い下線選択インジケーター、各種入力欄・ボタンの枠線や背景のプレミアム化）を本番の `ui/main_window.py` に完全移植・統合しました。
  * **バージョン表示の修正**: ウィンドウタイトルの表記を `Ver2.3.2` に更新しました。
* **対象コミット/対話**: 対話 bba790d6

### 🔧 v2.3.1 (2026-07-13)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: タブUIのブラッシュアップ、入力欄高さ調整、およびタイトルバージョン更新。
* **詳細内容**:
  * **タブの下線インジケーター化**: タブの背景色を透過にし、選択時に下線（インジケーター）が表示されるフラットでモダンなデザインへ刷新しました（ライト/ダークテーマ両対応）。
  * **プロンプト入力欄の高さ拡張**: 入力しやすさと右側ボタン群とのアライメント（高さの整合）を整えるため、プロンプト入力欄の高さを `70px` から `95px`（1行分増加）に、ネガティブプロンプト入力欄を `50px` から `60px` に拡張しました。
  * **バージョン表示の修正**: ウィンドウタイトルの表記を `Ver2.3.1` に更新しました。
* **対象コミット/対話**: 対話 bba790d6

### 🚀 v2.3.0 (2026-07-13)
* **分類**: 機能追加 (0.1アップ)
* **概要**: スタイルプリセットとクイックタグの統合、およびUI/UXのプレミアム化。
* **詳細内容**:
  * **データ・機能の一元化**: ハードコードされていた「Quick tags」を廃止し、スタイル定義をスタイルプリセット（`presets.json`）に一本化しました。
  * **プロンプト直接挿入機能**: 「Style Preset」のコンボボックスの隣に「✍️ Insert to Prompt」ボタンを追加し、選択したプリセットのキーワードをワンクリックでプロンプト入力欄の末尾に挿入できるようにしました。
  * **No Preset 時の安全ガード**: ドロップダウンで「プリセット無し」が選ばれている時は、「✍️ Insert to Prompt」ボタンが自動的に無効化（Disabled）される連動ロジックを実装しました。
  * **最下部パネルの整理**: クイックタグ廃止に伴い、最下部のグループボックスを「Prompt Helper Libraries」に改名。レイアウトを横並び（QHBoxLayout）に変更して高さを縮小し、キャラクターライブラリおよびプロダクトライブラリボタンをすっきりと配置しました。
  * **UIのプレミアム・ブラッシュアップ**:
    * 各種主要ボタン（Generate、新規作成、クリア、ライブラリボタンなど）に対し、ホバー（Hover）やプレス（Pressed）、無効化（Disabled）時の滑らかな色変化エフェクトを適用し、操作へのフィードバックを視覚的に洗練させました。
    * ライトモードとダークモードのどちらでも美しく調和するよう、透過ボーダーとシステムアクセントカラー（青・緑・オレンジ・赤）を採用しました。
* **対象コミット/対話**: 対話 bba790d6

### 🚀 v2.2.0 (2026-07-12)
* **分類**: 機能追加 (0.1アップ)
* **概要**: プロジェクト管理UIの強化と表示の改善。
* **詳細内容**:
  * `Project` タブに「Create Project」入力欄とボタン、および「Delete Project」ボタンを追加し、GUI上から直接プロジェクトを動的に作成・削除できるようになりました。
  * アクティブなプロジェクト名をプロンプト入力欄のすぐ上に常時表示するようにし、ユーザーが現在どのプロジェクトコンテキストで作業しているか視覚的にわかりやすくしました。
  * `Generation` タブの「New Project」ボタンを、機能的役割に合わせ「New Create (新規作成)」に表記変更しました。
* **対象コミット**: `dd4386b`

### 🔧 v2.1.1 (2026-07-12)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: レイアウトの微調整と選択表示の改善。
* **詳細内容**:
  * 操作ボタンの並び順を、上から「New Project」→「Generate」→「Clear Context（コンテキスト消去）」の順に変更し、ボタン内に文字が美しく収まるよう高さを調整しました。
  * ギャラリーのサムネイルカードを選択した際のアクティブハイライト（水色の外枠）を追加し、現在どの画像が選択されているかの視認性を向上させました。
* **対象コミット**: `209ade0`

### 🚀 v2.1.0 (2026-07-12)
* **分類**: 機能追加 (0.1アップ)
* **概要**: 生成結果の自動プレビュー化およびダブルクリックオープン機能の実装。
* **詳細内容**:
  * 画像生成が完了した際、自動的に中央のメインプレビュー領域に最新の画像が反映されるようになりました。
  * メインプレビュー画像、およびギャラリーサムネイル画像をダブルクリックすることで、OSの既定の画像ビューアーで画像ファイルを直接開くことができるようになりました。
* **対象コミット**: `d02ba7e`

### 🔧 v2.0.1 (2026-07-12)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: リファクタリング後のデバッグとクリーンアップ。
* **詳細内容**:
  * 大規模リファクタリング後に発生していた画像生成プロセスの不整合（および検証コードとの競合）を解消しました。
  * 「Clear Context」ボタンが機能していなかったバグを特定し修正しました。
  * コンテキストをクリアした際などに、メインプレビュー表示がクリーンアップされず前回の画像が残り続ける問題を修正しました。
* **対象コミット**: `3f26cf9` 後続

### 🔄 v2.0.0 (2026-07-12)
* **分類**: ソースコード全体の書き直し (1.0.0アップ)
* **概要**: Ver1.1 モジュール化アーキテクチャへの大規模リファクタリング。
* **詳細内容**:
  * アプリケーションのコードベース肥大化に伴い、長期的な保守性と拡張性を高めるため、モジュール分割リファクタリングを実施。
  * `core` (システムバス・コーディネーター), `services` (ビジネスロジック各種), `database` (接続・スキーマ管理), `ui` (メインウィンドウおよび各パネル/ウィジェット) へのディレクトリ構成分割を行い、結合度を下げました。
* **対象コミット**: `3f26cf9`

### 🚀 v1.5.0 (2026-07-11)
* **分類**: 機能追加 (0.1アップ)
* **概要**: ドラッグ＆ドロップによる画像参照編集機能の実装。
* **詳細内容**:
  * ギャラリー内の画像をプレビュー領域などへドラッグ＆ドロップすることで、その画像を参照画像として設定し、画像編集（Image-to-Imageやバリエーション生成など）を行える機能を追加しました。

### 🚀 v1.4.0 (2026-07-11)
* **分類**: 機能追加 (0.1アップ)
* **概要**: 既存プリセット編集の初期化機能と、品質設定ラジオボタンの実装。
* **詳細内容**:
  * 既存のスタイルプリセットも編集できるようにし、かつ編集した内容を「初期化」ボタンで即座に元の定義状態に戻せる安全性を確保しました。
  * `Generation Parameters` に品質変更オプションとして「Low, Medium, High」を指定できるラジオボタンを設置しました。

### 🔧 v1.3.1 (2026-07-11)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: ギャラリーUI of ビジュアル調整。
* **詳細内容**:
  * ギャラリーのサムネイルカードの背景色をグレーからホワイトに変更し、モダンな外観に調整しました。
  * サムネイル右上にある一括削除用のチェックボックスサイズを 1.25 倍に拡大し、チェックしやすさを向上させました。

### 🚀 v1.3.0 (2026-07-10)
* **分類**: 機能追加 (0.1アップ)
* **概要**: ギャラリー画像の一括選択・削除機能の実装。
* **詳細内容**:
  * ギャラリー内の画像を複数選択して一括削除できる機能を実装しました。サムネイルの右上にチェックボックスを追加し、チェックしたものをまとめて削除できます。

### 🔧 v1.2.1 (2026-07-10)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: 画像生成エラーおよびAPIキー連携バグのデバッグ・修正。
* **詳細内容**:
  * 画像生成実行時の各種エラーをデバッグし、正しく生成プロセスが完走するようAPI呼び出しやエラーハンドリングを修正しました。
  * 入力されたAPIキーの連携処理を修正し、認証エラーを防ぐ実装を行いました。

### 🚀 v1.2.0 (2026-07-10)
* **分類**: 機能追加 (0.1アップ)
* **概要**: 各種ユーティリティ機能（テンプレート・パラメータ保存・リテイク・画像比較・料金表示など）の大幅拡張。
* **詳細内容**:
  * プロンプトおよびネガティブプロンプトのテンプレート保存・管理機能を追加。
  * 生成した画像ごとに設定されていた生成パラメータ（シード、プロンプト、サイズ等）をメタデータとして保存・復元できるようにしました。
  * ワンクリックで同じ設定で再度生成する「リテイク」機能を実装しました。
  * お気に入り（マーク）機能を追加しました。
  * APIの使用料金をリアルタイムで算出・表示する機能およびアカウント残高表示機能を実装しました。
  * 2枚の画像を左右に並べて表示・比較できる「画像比較」機能を実装しました。

### 🔧 v1.1.1 (2026-07-10)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: ダークモード切替バグの修正。
* **詳細内容**:
  * 設定画面でダークモードからライトモードに切り替えた直後に、GUIの一切の入力を受け付けなくなる重篤なフリーズバグを特定し、テーマ適用ロジックを修正しました。

### 🚀 v1.1.0 (2026-07-10)
* **分類**: 機能追加 (0.1アップ)
* **概要**: 設定画面のメニュー設置、テーマ切替、スタイルプリセット管理の基本実装。
* **詳細内容**:
  * アプリケーションメニューから「設定画面」を呼び出せるように実装。
  * 設定画面内にAPIキー入力・保存機能を実装。
  * アプリ全体のライトモード/ダークモード切り替え機能を実装。
  * スタイルプリセット（no presetを含む）の編集・追加・削除ができるよう管理ダイアログを実装。

### 🔧 v1.0.1 (2026-07-10)
* **分類**: 細かな修正 (0.0.1アップ)
* **概要**: 生成モデル設定の修正。
* **詳細内容**:
  * OpenAIの提供する画像生成モデルの設定を、従来のdall-e-2から、指示に基づき最新の「GPT Image 2」モデルを利用するようコードを修正しました。

### 📦 v1.0.0 (2026-07-10)
* **分類**: 初期リリース (最初のコーディング)
* **概要**: OpenAI Image Studio の基本機能とGUIの構築。
* **詳細内容**:
  * PyQtを用いたデスクトップ向けの画像生成スタジオ「OpenAI Image Studio」の作成を開始。
  * プロンプト入力や画像サイズ設定を行う「レフトUI」、生成画像を表示する「中央プレビュー」、画像履歴や設定を切り替える「ライトUI」など、基本的なUIレイアウトと生成パイプラインの基礎を構築しました。
